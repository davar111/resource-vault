{
  "name": "resource-vault",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "node scripts/check-mojibake.mjs && node scripts/check-no-native-dialogs.mjs"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
